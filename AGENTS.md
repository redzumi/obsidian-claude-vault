# AGENTS.md

Instructions for OpenAI Codex CLI.

Read `_init.md` first and only `_init.md`.

Lazy loading:
1. `_context/_session-context.md` — if current project status is needed.
2. `_context/_personal-context.md` — if personal constraints / integrations are needed.
3. `_context/_conventions-quick.md` — for structure / formatting tasks only.

## Notes

- Ritual prompts (`/morning`, `/dayend`, `/weekly-review`) are currently Claude Code slash commands.
  See `.claude/commands/` for their logic — adapt as needed for Codex workflows.
- The full vault structure (PARA, `_context/`, `_focus.md`) is tool-agnostic and works as-is.
